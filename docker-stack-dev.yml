version: '3.5'

services:
  db:
    image: mdillon/postgis:10-alpine
    deploy:
      placement:
        constraints: [node.role == manager]
      restart_policy:
        condition: any
        delay: 0s
        max_attempts: 3
        window: 60s
      resources:
        limits:
          cpus: '.5'
          memory: 512M
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: django
      POSTGRES_PASSWORD: ***PASSWORD***
      PGDATA: /var/lib/postgresql/data/pgdata

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:5.6.8
    deploy:
      restart_policy:
        condition: any
        delay: 0s
        max_attempts: 3
        window: 60s
      resources:
        limits:
          cpus: '.5'
          memory: 512M
    ports:
      - 9200:9200
      - 9300:9300
    volumes:
      - esdata:/usr/share/elasticsearch/data
    environment:
      - "ES_JAVA_OPTS=-Xms256m -Xmx256m"
      - discovery.type=single-node
      - xpack.security.enabled=false

  django:
    image: registry.gitlab.com/bentrm/lis:latest
    tty: true
    deploy:
      restart_policy:
        condition: any
        delay: 0s
        max_attempts: 3
        window: 60s
      resources:
        limits:
          cpus: '.5'
          memory: 512M
    ports:
      - 8000:8000
    depends_on:
      - db
      - elasticsearch
    volumes:
      - static:/html/static
      - media:/html/media
      - type: bind
        source: ./src
        target: /src
    environment:
      SECRET_KEY: ***SECRET***
      STATIC_ROOT: /html/static
      STATIC_URL: /static/
      MEDIA_ROOT: /html/media
      MEDIA_URL: /media/
      DB_ENGINE: django.contrib.gis.db.backends.postgis
      DB_NAME: django
      DB_USER: django
      DB_PASSWORD: ***PASSWORD***
      DB_HOST: db
      DB_PORT: 5432
    command: sh -c "while ! nc -w 1 -z db 5432; do sleep 1; done; /venv/bin/python ./manage.py runserver 0.0.0.0:8000;"

  web:
    image: nginx:alpine
    deploy:
      restart_policy:
        condition: any
        delay: 0s
        max_attempts: 3
        window: 60s
      resources:
        limits:
          cpus: '.25'
          memory: 256M
    volumes:
      - static:/usr/share/nginx/html/static:ro
      - media:/usr/share/nginx/html/media:ro
    ports:
      - 8001:80

volumes:
  pgdata:
  esdata:
  static:
  media:
