version: '3.7'

services:
  app:
    build:
      context: ./app
      target: builder
    command: ["npm", "start"]
    ports:
      - "3000:3000"
    volumes:
      - ./app/src:/app/src
  cms:
    build: ./cms
    environment:
      DEBUG: 1
    volumes:
      - ./cms:/app
